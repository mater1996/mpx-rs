{
  "private": true,
  "name": "@mpxjs-rs/compiler-monorepo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "ava"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.16.1",
    "ava": "^5.1.1",
    "benchmark": "^2.1.4"
  },
  "engines": {
    "node": ">= 10"
  }
}
